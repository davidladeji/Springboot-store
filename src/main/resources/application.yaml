spring:
    application:
        name: store
    datasource:
        url: jdbc:mysql://localhost:3306/store?createDatabaseIfNotExist=true
        username: root
        password: L1ftOff$
    flyway:
        baseline-on-migrate: true
    jpa:
        show-sql: true